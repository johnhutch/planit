<%= @plan.title %>

<%= @plan.planner.email %> wants to hang out.

Visit the following URL to figure out the particulars:
<%= respond_plan_url(@token) %>

Brought to you by <%= t('site.name') %>, on the web at <%= t('site.url') %>
